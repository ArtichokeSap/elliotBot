# Core data processing and analysis
# Updated to secure versions (2026-01-29)
numpy>=1.24.0  # Security fixes for older versions
pandas>=1.5.0  # Multiple security patches
scipy>=1.10.0
yfinance>=0.2.28  # Recent bug fixes and rate limiting improvements

# Optional: Cryptocurrency data (install separately if needed)
# ccxt>=4.0.0  # For cryptocurrency exchanges

# Optional: TA-Lib (install manually if needed)
# ta-lib>=0.4.0  # Requires manual installation on Windows

# Machine Learning
scikit-learn>=1.2.0  # Security improvements

# Optional ML libraries (install separately if needed)
# tensorflow>=2.13.0  # For advanced ML models
# torch>=1.11.0       # For PyTorch models  
# xgboost>=1.6.0      # For gradient boosting

# Technical Analysis
# ta>=0.10.0          # Optional: additional TA library
# stockstats>=0.5.0   # Optional: alternative TA library

# Visualization
plotly>=5.14.0  # Security and performance updates
matplotlib>=3.7.0  # Multiple CVE fixes
seaborn>=0.12.0  # Updated for compatibility

# Optional visualization libraries
# mplfinance>=0.12.9b7  # For candlestick charts (beta version)

# Optional Web Framework
# streamlit>=1.28.0     # For web dashboard
# fastapi>=0.95.0       # For REST API
# uvicorn>=0.20.0       # For FastAPI server

# Data Storage
sqlalchemy>=2.0.0  # Major version with security improvements (CVE-2023-30608 fix)

# Utilities
python-dotenv>=0.19.0
joblib>=1.3.0  # Used for secure model serialization (replaces pickle)
tqdm>=4.64.0
requests>=2.31.0  # CVE-2023-32681 fix (critical)
pyyaml>=6.0.1  # CVE-2020-14343 fix (always use safe_load)

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
